# Bu dosyayı "secrets.properties" olarak kopyala (aynı klasöre) ve
# kendi Google Maps API key'ini yaz. secrets.properties git'e ASLA
# commit edilmez (bkz. .gitignore) — bu örnek dosya sadece şablon.
MAPS_API_KEY=buraya-kendi-api-key-ini-yaz
